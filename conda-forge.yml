appveyor:
  secure: {BINSTAR_TOKEN: <will be updated by "conda-smithy register-feedstock-ci">}
channels:
  sources: ['owner, e.g. "conda-forge"']
  targets:
  - ['<owner, e.g. "conda-forge">', '<channel, e.g. "main">']
is_multi: true
recipe_dir: recipes
travis:
  secure: {BINSTAR_TOKEN: <will be updated by "conda-smithy register-feedstock-ci">}
